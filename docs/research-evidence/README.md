# Research 证据

本目录只保存支持调研或后续验证所必需的证据。

## 2026-08-14 访问限制

In-app Browser 无法完成到 `https://www.creen.ai/` 的导航。对 Home、`/create`、`/pricing`、`/robots.txt` 和 `/sitemap.xml` 的直接 HTTP `HEAD` 请求均返回 Cloudflare `403`，因此没有把任何 Creen 视觉截图描述为成功页面采集。

公开页面内容来自报告 2026-08-13 或 2026-08-14 抓取日期的当前搜索抓取器；Source URL 仍是官方 `www.creen.ai`，并记录在 `../evidence-index.md`。该证据只能支持内容与可发现性结论，不能支持像素级或登录后页面结论。
